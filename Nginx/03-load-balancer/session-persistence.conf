# Session Persistence Configuration
# Ensures same client goes to same backend server

# IP Hash Method (Open Source)
upstream backend {
    ip_hash;  # Same IP always goes to same server
    
    server 192.168.1.10:8080;
    server 192.168.1.11:8080;
    server 192.168.1.12:8080;
}

server {
    listen 80;
    server_name example.com;
    
    location / {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}

# Cookie-based Sticky Sessions (Nginx Plus)
# upstream backend {
#     sticky cookie srv_id expires=1h domain=.example.com path=/;
#     server 192.168.1.10:8080;
#     server 192.168.1.11:8080;
#     server 192.168.1.12:8080;
# }

# Note: Best practice is to use shared session storage (Redis, database)
# instead of sticky sessions for better scalability

